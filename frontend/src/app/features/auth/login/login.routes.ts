import { Routes } from "@angular/router";
import { LoginComponent } from "./login";

/* Definisce la rotta associata al componente di login. */
export const loginRoutes: Routes = [
    { path: '', component: LoginComponent },
]